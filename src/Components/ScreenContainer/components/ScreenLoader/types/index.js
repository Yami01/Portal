import type {ViewStyleProp} from "react-native/Libraries/StyleSheet/StyleSheet";

/**
 *
 * @format
 *
 */

export type PropsType = {
  showLoader: boolean;
}

export type ScreenLoaderStylesType = {
  container: ViewStyleProp,
  lottieView: ViewStyleProp,
}