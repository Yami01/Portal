/**
 *
 * @format
 *
 */

import {StyleSheet} from 'react-native'
import {Colors} from '@/Theme/Variables'

const HeaderCenterStyles: HeaderCenterStyleTypes = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
  },
  title: {
    color: Colors.black,
  },
})

export default HeaderCenterStyles
